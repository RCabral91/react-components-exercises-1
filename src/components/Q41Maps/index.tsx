import { useCallback, useState } from 'react';

const Q42OneMoreLine: React.FC = () => {
    return (
        <div>
            <input type="text" />
            <input type="text" />
            <button type="button">Procurar</button>
        </div>
    );
};

export default Q42OneMoreLine;
